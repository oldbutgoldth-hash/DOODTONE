// Control fixture: a normal, valid ES module with no defects. Included
// so the regression test proves the gate does not over-flag valid code.
export function add(a, b) {
  return a + b;
}
export const CONSTANT_VALUE = 42;
