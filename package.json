{
  "name": "lumixa_ai_epic_2e_j_phase_c_recovered_deadcode_clean",
  "version": "1.0.0",
  "description": "This is a **drop-in replacement** for your original `PresetForge AI` static site — not a mockup. All 30 analysis engines in `core/` are byte-for-byte unchanged from your codebase (verified via diff — Stage 2.4.2B.1); `ui/app.js` and `ui/ui-engine.js` are your original controller logic with only the styling mechanism swapped (inline CSS custom properties instead of `styles.css`); every canvas renderer (`ui/*-renderer.js`) is untouched.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "npm run test:static",
    "dev": "node tools/local-static-server.mjs",
    "test:syntax": "node tools/esm-syntax-gate.mjs",
    "test:static": "node qa/run-static-suites.mjs",
    "test:browser": "node qa/run-browser-suites.mjs",
    "test:browser:report": "node qa/run-browser-suites.mjs --report",
    "test:local-gate": "node tools/local-gate.mjs",
    "test:deploy": "node qa/epic-2e-j-deploy-preview-geometry-test.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
